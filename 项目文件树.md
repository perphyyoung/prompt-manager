# 项目文件树

## 主进程

```bash
main/
├── index.ts                     # 应用主进程入口，管理窗口生命周期和 IPC 通信
├── database.ts                  # 数据库操作层，封装 SQLite 数据存取
├── database-types.ts            # 数据库类型定义
├── database-errors.ts           # 数据库错误类定义
├── logger.ts                    # 日志服务，统一输出到 pm.log
└── ConfigManager.ts             # 配置管理器
```

## 渲染进程

```bash
renderer/
├── app.ts                       # 应用主类，协调各管理器，处理全局状态和事件
├── app.types.ts                 # App 类型定义
│
├── config/                      # 配置目录
│   ├── index.ts                 # 配置统一导出
│   └── BatchConfig.ts           # 批量操作配置
│
├── managers/                    # 管理器层 - 业务逻辑核心
│   ├── index.ts                 # 管理器统一导出
│   │
│   ├── PanelManagerBase.ts      # 面板管理器基类，提供列表/网格视图通用功能
│   ├── PromptPanelManager.ts    # 提示词面板管理器，管理提示词列表展示
│   ├── ImagePanelManager.ts     # 图像面板管理器，管理图像列表展示
│   │
│   ├── PromptDetailManager.ts   # 提示词详情管理器，管理提示词编辑模态框
│   ├── ImageDetailManager.ts    # 图像详情管理器，管理图像详情模态框
│   ├── DetailViewManager.ts     # 详情视图管理器基类
│   │
│   ├── ImageFullscreenManager.ts    # 图像全屏查看管理器
│   ├── ImageSelectorManager.ts      # 图像选择器管理器，提示词关联图像选择
│   ├── ImageUploadManager.ts        # 图像上传管理器，处理图像上传流程
│   ├── ImagePreviewManager.ts       # 图像预览管理器，负责预览渲染和交互
│   ├── ImageContextMenuManager.ts   # 图像右键菜单管理器
│   │
│   ├── NewPromptManager.ts      # 新建提示词管理器，管理新建提示词页面
│   ├── TrashManager.ts          # 回收站管理器，统一管理提示词和图像回收站
│   │
│   ├── SimpleTagManager.ts          # 简单标签管理器，基础标签功能
│   ├── SimpleTagManagerFactory.ts   # 简单标签管理器工厂，创建标签管理器实例
│   ├── TagRegistry.ts               # 标签注册表，管理标签数据和业务逻辑
│   ├── TagService.ts                # 标签服务，封装标签相关 API 调用
│   ├── TagUI.ts                     # 标签 UI 组件，生成标签 HTML 和渲染
│   ├── TagGroupModalManager.ts      # 标签组模态框管理器
│   ├── TopGroupManager.ts           # 顶部标签组管理器
│   │
│   ├── BatchProcessor.ts            # 批量操作处理器，执行批量添加标签/收藏等操作
│   ├── BatchToolbarUI.ts            # 批量操作工具栏 UI 控制器
│   ├── SearchSortManager.ts         # 搜索排序管理器，处理搜索和排序逻辑
│   │
│   ├── NavigationManager.ts         # 导航管理器，处理面板切换导航
│   ├── ToolbarManager.ts            # 工具栏管理器，管理顶部工具栏
│   ├── ModalManager.ts              # 模态框管理器，通用模态框控制
│   ├── ToastManager.ts              # 提示管理器，显示操作提示
│   ├── SettingsManager.ts           # 设置管理器，管理应用设置
│   ├── ImportExportManager.ts       # 导入导出管理器，处理数据导入导出
│   │
│   ├── handlers/                    # 回收站处理器
│   │   ├── index.ts                 # 处理器统一导出
│   │   ├── TrashHandler.ts          # 回收站处理器基类
│   │   ├── PromptTrashHandler.ts    # 提示词回收站处理器
│   │   └── ImageTrashHandler.ts     # 图像回收站处理器
│   │
│   └── SharedComponents/            # 共享组件
│       ├── index.ts                 # 共享组件统一导出
│       ├── ButtonFactory.ts         # 按钮工厂，创建各类卡片按钮
│       ├── CardConfig.ts            # 卡片配置类，定义卡片结构和行为
│       ├── ListConfig.ts            # 列表配置类，定义列表项结构和行为
│       ├── PanelRenderer.ts         # 面板渲染器，渲染列表/网格视图容器
│       ├── UnifiedCardRenderer.ts   # 统一卡片渲染器，渲染所有类型卡片
│       └── UnifiedListRenderer.ts   # 统一列表渲染器，渲染所有类型列表项
│
├── components/                  # 组件层
│   ├── index.ts                 # 组件统一导出
│   ├── BatchTagManager.ts       # 批量标签管理组件，支持批量删除标签
│   ├── EditableTagList.ts       # 可编辑标签列表组件，支持增删改标签
│   └── ProgressDialog.ts        # 进度对话框组件
│
├── services/                    # 服务层
│   ├── index.ts                 # 服务统一导出
│   ├── DataClearApi.ts          # 数据清理 API 抽象接口
│   ├── DataClearIpcService.ts   # 数据清理 IPC 服务
│   ├── DialogService.ts         # 对话框服务，管理确认对话框
│   ├── ElectronDataClearApi.ts  # Electron 数据清理 API 实现
│   ├── ElectronTagApi.ts        # Electron 标签 API 实现
│   ├── ImageUploadService.ts    # 图像上传服务，核心上传逻辑
│   ├── MockDataClearApi.ts      # Mock 数据清理 API 实现
│   ├── MockTagApi.ts            # Mock 标签 API 实现
│   ├── SafeRatingService.ts     # 安全评级服务，处理内容安全过滤
│   ├── TagApi.ts                # 标签 API 抽象接口
│   ├── TagAutocomplete.ts       # 标签自动完成服务，提供标签输入自动完成功能
│   ├── TagIpcService.ts         # 标签 IPC 服务
│   ├── UploadNotificationService.ts # 上传通知服务，处理上传成功/失败通知
│   └── UploadStrategies.ts      # 上传策略，DelaySaveStrategy/DirectSaveStrategy
│
├── renderer_utils/              # 渲染进程工具类
│   ├── index.ts                 # 工具类统一导出
│   ├── DomUtils.ts              # DOM 工具类
│   ├── HoverTooltipManager.ts   # 悬停提示管理器，处理鼠标悬停提示
│   ├── SaveManager.ts           # 保存管理器，自动保存表单变更
│   ├── SaveStrategy.ts          # 保存策略
│   └── ShortcutManager.ts       # 快捷键管理器，处理键盘快捷键绑定
│
└── types/
    └── TagTypes.ts              # 标签类型定义
```

## Preload 脚本

```bash
preload/
└── index.ts                     # Preload 脚本入口，定义 IPC 通道
```

## 类型定义

```bash
types/
├── entities.ts                  # 实体类型定义
└── electron-api.d.ts            # Electron API 类型定义
```

## 项目根目录工具

```bash
src/utils/                       # 共享工具类
├── index.ts                     # 工具类统一导出
├── CacheManager.ts              # 缓存管理器，管理数据缓存
├── DuplicatePrevention.ts       # 防重复提交工具
├── EventBus.ts                  # 事件总线，提供发布订阅模式
├── FileUtils.ts                 # 文件工具类，提供复制目录等功能
├── HtmlUtils.ts                 # HTML 工具类，提供 escapeHtml、formatFileSize 等方法
├── idGenerator.ts               # ID 生成器，生成唯一 ID
├── isSameId.ts                  # ID 比较工具，统一处理 ID 类型比较
├── ListNavigator.ts             # 列表导航器，处理列表项键盘导航
├── LRUCache.ts                  # LRU 缓存实现，有限容量缓存
├── TextUtils.ts                 # 文本工具类
└── TimeUtils.ts                 # 时间工具类

src/constants.ts                 # 应用常量定义
```

## 文件统计

| 目录 | 文件数 | 说明 |
|------|--------|------|
| 主进程 | 6 | index.ts, database.ts, database-types.ts, database-errors.ts, logger.ts, ConfigManager.ts |
| preload | 1 | index.ts |
| types | 2 | entities.ts, electron-api.d.ts |
| renderer/config | 2 | 配置目录（含 index.ts） |
| renderer/managers | 31 | 业务管理器（含 index.ts） |
| renderer/managers/handlers | 4 | 回收站处理器（含 index.ts） |
| renderer/managers/SharedComponents | 7 | 共享组件（含 index.ts） |
| renderer/renderer_utils | 6 | 渲染进程工具类（含 index.ts） |
| renderer/components | 4 | UI 组件（含 index.ts） |
| renderer/services | 14 | 服务类（含 index.ts） |
| renderer/types | 1 | TagTypes.ts |
| src/utils | 12 | 共享工具类（含 index.ts） |
| 根目录 | 1 | constants.ts |
| **总计** | **91** | **全部为 TypeScript** |

## 架构分层

```bash
┌─────────────────────────────────────┐
│  components/     UI 组件层          │
├─────────────────────────────────────┤
│  managers/      业务逻辑层          │
├─────────────────────────────────────┤
│  services/      服务层              │
├─────────────────────────────────────┤
│  utils/         工具类层            │
├─────────────────────────────────────┤
│  main/          主进程              │
│  database.ts    数据层              │
└─────────────────────────────────────┘
```

## 日志系统

```bash
logger.ts                        # 统一日志服务
├── 输出到控制台                   # 实时查看
├── 输出到 pm.log                 # 持久化记录（rootDir 下）
└── IPC: renderer-log            # 渲染进程日志通道
```
