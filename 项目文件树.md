# 项目文件树

## 主进程

```bash
main/
├── index.ts                     # 应用主进程入口，管理窗口生命周期和 IPC 通信
├── mainLogger.ts                # 日志服务，统一输出到 pm.log（基于 electron-log）
├── database.ts                  # 数据库操作层，封装 SQLite 数据存取
├── database-types.ts            # 数据库类型定义
└── database-errors.ts           # 数据库错误类定义
```

## 中间层

```bash
middle/
└── BatchToolbarMiddle.ts        # 批量操作工具栏中间层，协调选择状态与 UI

pyBatchToolbar/
├── index.ts                     # 批量工具栏统一导出
├── presets.ts                   # 工具栏按钮预设配置
├── types.ts                     # 类型定义
└── utils.ts                     # 工具函数

pyTagGroups/
├── index.ts                     # 标签组统一导出
├── dataAccess.ts                # 标签组数据访问层
├── operations.ts                # 标签组业务操作
├── PyTagGroups.ts               # 标签组核心类
├── TopGroupManager.ts           # 顶部标签组管理器
├── types.ts                     # 类型定义
├── utils.ts                     # 工具函数
└── validation.ts                # 校验逻辑
```

## 渲染进程

```bash
renderer/
├── app.ts                       # 应用主类，协调各管理器，处理全局状态和事件
├── app.types.ts                 # App 类型定义
├── index.html                   # 页面结构
└── styles.css                   # 全局样式
│
├── configs/                     # 配置目录
│   └── LocalStorageConfig.ts    # localStorage 配置注册与校验
│
├── managers/                    # 管理器层 - 业务逻辑核心
│   ├── index.ts                 # 管理器统一导出
│   │
│   ├── PanelManagerBase.ts      # 面板管理器基类，提供网格视图通用功能（虚拟滚动/分页/选择）
│   ├── PromptPanelManager.ts    # 提示词面板管理器，管理提示词展示
│   ├── ImagePanelManager.ts     # 图像面板管理器，管理图像展示
│   │
│   ├── PromptDetailManager.ts   # 提示词详情管理器，管理提示词编辑模态框
│   ├── ImageDetailManager.ts    # 图像详情管理器，管理图像详情模态框
│   ├── DetailViewManager.ts     # 详情视图管理器基类
│   ├── DetailTagController.ts   # 详情标签控制器，详情页标签增删逻辑
│   │
│   ├── TagManager.ts            # 标签管理器
│   ├── PromptTagManager.ts      # 提示词标签管理器
│   ├── ImageTagManager.ts       # 图像标签管理器
│   ├── TagUI.ts                 # 标签 UI 组件，生成标签 HTML 和渲染
│   │
│   ├── ImageFullscreenManager.ts    # 图像全屏查看管理器
│   ├── ImageSelectorManager.ts      # 图像选择器管理器，提示词关联图像选择
│   ├── ImageUploadManager.ts        # 图像上传管理器，处理图像上传流程
│   ├── ImagePreviewManager.ts       # 图像预览管理器，负责预览渲染和交互
│   │
│   ├── NewPromptManager.ts      # 新建提示词管理器，管理新建提示词页面
│   ├── TrashManager.ts          # 回收站管理器，统一管理提示词和图像回收站
│   │
│   ├── SearchSortManager.ts         # 搜索排序管理器，处理搜索、排序与卡片尺寸滑块
│   ├── NavigationManager.ts         # 导航管理器，处理面板切换导航
│   ├── ToolbarManager.ts            # 工具栏管理器，管理顶部工具栏
│   ├── ModalManager.ts              # 模态框管理器，通用模态框控制
│   ├── ToastManager.ts              # 提示管理器，显示操作提示
│   ├── SettingsManager.ts           # 设置管理器，管理应用设置
│   ├── StatisticsManager.ts         # 统计管理器
│   ├── ImportExportManager.ts       # 导入导出管理器，处理数据导入导出
│   ├── ContextStackManager.ts       # 上下文栈管理器，处理 Esc 键关闭嵌套 UI
│   │
│   ├── handlers/                    # 回收站处理器
│   │   ├── index.ts                 # 处理器统一导出
│   │   ├── TrashHandler.ts          # 回收站处理器基类
│   │   ├── TrashTypes.ts            # 回收站类型定义
│   │   ├── PromptTrashHandler.ts    # 提示词回收站处理器
│   │   └── ImageTrashHandler.ts     # 图像回收站处理器
│   │
│   ├── SharedComponents/            # 共享组件
│   │   ├── index.ts                 # 共享组件统一导出
│   │   ├── ButtonFactory.ts         # 按钮工厂，创建各类卡片按钮
│   │   ├── CardConfig.ts            # 卡片配置类，定义卡片结构和行为
│   │   ├── PanelRenderer.ts         # 面板渲染器，空状态显示等容器辅助方法
│   │   └── UnifiedCardRenderer.ts   # 统一卡片渲染器，渲染所有类型卡片
│   │
│   └── Strategies/                  # 事件策略
│       ├── BaseEventStrategy.ts     # 事件策略基类，事件委托处理点击与复选框
│       └── IEventStrategy.ts        # 事件策略接口定义
│
├── components/                  # 组件层
│   ├── index.ts                 # 组件统一导出
│   ├── EditableTagList.ts       # 可编辑标签列表组件，支持增删改标签
│   └── ProgressDialog.ts        # 进度对话框组件
│
├── services/                    # 服务层
│   ├── index.ts                 # 服务统一导出
│   ├── DataClearApi.ts          # 数据清理 API 抽象接口
│   ├── DataClearIpcService.ts   # 数据清理 IPC 服务
│   ├── DialogService.ts         # 对话框服务，管理确认对话框
│   ├── ElectronDataClearApi.ts  # Electron 数据清理 API 实现
│   ├── ImageUploadService.ts    # 图像上传服务，核心上传逻辑
│   ├── MockDataClearApi.ts      # Mock 数据清理 API 实现
│   ├── TagAutocomplete.ts       # 标签自动完成服务，提供标签输入自动完成功能
│   ├── TagService.ts            # 标签服务，封装标签相关 API 调用
│   └── UploadStrategies.ts      # 上传策略，DelaySaveStrategy/DirectSaveStrategy
│
└── renderer_utils/              # 渲染进程工具类
    ├── index.ts                 # 工具类统一导出
    ├── ContextMenuUtils.ts      # 右键菜单工具，动态生成上下文菜单
    ├── DomUtils.ts              # DOM 工具类
    ├── ErrorHandler.ts          # 错误处理器
    ├── HoverTooltipManager.ts   # 悬停提示管理器，处理鼠标悬停提示
    ├── SaveManager.ts           # 保存管理器，自动保存表单变更
    ├── SaveStrategy.ts          # 保存策略
    ├── ShortcutManager.ts       # 快捷键管理器，处理键盘快捷键绑定
    ├── VirtualScrollBar.ts      # 虚拟滚动条组件
    ├── VirtualScroller.ts       # 虚拟滚动控制器
    └── VirtualWindowRenderer.ts # 虚拟窗口渲染器，视口内按需渲染卡片
```

## Preload 脚本

```bash
preload/
└── index.ts                     # Preload 脚本入口，定义 IPC 通道
```

## 类型定义

```bash
types/
└── entities.ts                  # 实体类型定义
```

## 项目根目录工具

```bash
src/utils/                       # 共享工具类
├── index.ts                     # 工具类统一导出
├── CacheManager.ts              # 缓存管理器，管理数据缓存
├── debounce.ts                  # 防抖工具
├── DuplicatePrevention.ts       # 防重复提交工具
├── EventBus.ts                  # 事件总线，提供发布订阅模式
├── FileUtils.ts                 # 文件工具类，提供复制目录等功能
├── HashUtils.ts                 # 哈希工具，提供 cyrb53 指纹算法
├── HtmlUtils.ts                 # HTML 工具类，提供 escapeHtml、formatFileSize 等方法
├── idGenerator.ts               # ID 生成器，生成唯一 ID
├── ListNavigator.ts             # 列表导航器，处理列表项键盘导航
├── LocalStorageManager.ts       # localStorage 管理器，带配置注册与校验
├── Logger.ts                    # 日志工具
├── LRUCache.ts                  # LRU 缓存实现，有限容量缓存
├── TextUtils.ts                 # 文本工具类
└── TimeUtils.ts                 # 时间工具类，提供 dbTime/formatDbTimeToLocal

src/constants.ts                 # 应用常量定义
```

## 文件统计

| 目录 | 文件数 | 说明 |
|------|--------|------|
| main | 5 | index.ts, MainLogger.ts, database.ts, database-types.ts, database-errors.ts |
| middle | 1 | BatchToolbarMiddle.ts |
| pyBatchToolbar | 4 | 批量工具栏（含 index.ts） |
| pyTagGroups | 8 | 标签组模块（含 index.ts） |
| preload | 1 | index.ts |
| types | 1 | entities.ts |
| renderer/configs | 1 | LocalStorageConfig.ts |
| renderer/managers | 26 | 业务管理器（含 index.ts） |
| renderer/managers/handlers | 5 | 回收站处理器（含 index.ts） |
| renderer/managers/SharedComponents | 5 | 共享组件（含 index.ts） |
| renderer/managers/Strategies | 2 | 事件策略 |
| renderer/components | 3 | UI 组件（含 index.ts） |
| renderer/services | 10 | 服务类（含 index.ts） |
| renderer/renderer_utils | 11 | 渲染进程工具类（含 index.ts） |
| src/utils | 15 | 共享工具类（含 index.ts） |
| 根目录 | 1 | constants.ts |
| **总计** | **99** | |

## 架构分层

```bash
┌─────────────────────────────────────┐
│  components/     UI 组件层          │
├─────────────────────────────────────┤
│  managers/      业务逻辑层          │
├─────────────────────────────────────┤
│  middle / pyBatchToolbar /         │
│  pyTagGroups    批量与标签组中间层   │
├─────────────────────────────────────┤
│  services/      服务层              │
├─────────────────────────────────────┤
│  utils/         工具类层            │
├─────────────────────────────────────┤
│  main/          主进程              │
│  database.ts    数据层              │
└─────────────────────────────────────┘
```
