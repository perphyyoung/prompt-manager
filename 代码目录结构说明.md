# 代码目录结构说明

> 分层规则:依赖方向单向 `interfaces(视图/IPC) → application(用例) → domain(领域) ← infrastructure(基础设施)`,domain 不依赖任何外层。
> 配套文档:`docs/260829-架构重构报告.md`(重构过程与决策记录)。

---

## 一、目录结构

```
src/
├── main/                          # 主进程
│   ├── index.ts                   # 入口:registerAllIpc() + startApp(),不写业务
│   ├── bootstrap.ts               # 窗口/托盘/生命周期/单实例锁(组合根 + 环境适配)
│   ├── runtime.ts                 # 主进程模块级状态(mainWindow/dataDir 等),只经访问器读写
│   ├── database.ts                # 数据库 facade:聚合 5 个仓库的导出面,路由层从这里调用
│   ├── mainLogger.ts              # 主进程日志
│   ├── application/               # 【应用层】用例编排(跨仓储/事务性流程)
│   │   ├── index.ts                    # 组合根:用例服务的单例装配(新服务在此注册一行)
│   │   ├── ImportFullBackupService.ts  # 导入完整备份:校验→备份当前数据→恢复→失败自动回滚(依赖全部注入,回滚可单测)
│   │   ├── ExportFullBackupService.ts  # 导出完整备份:统计→manifest→复制库/图像→压缩ZIP(依赖全部注入)
│   │   └── ExportOrphanFilesService.ts  # 导出孤儿文件:扫描→逐个复制/删除(单文件失败不中断,计数语义)
│   ├── interfaces/                # 【接口适配层】
│   │   ├── ipc/                   # 6 个按域路由 + handleTyped.ts(类型化注册助手)
│   │   └── …                      # 路由只做:文件对话框 → 调用用例 → 错误翻译 → 回包
│   └── infrastructure/            # 【基础设施层】
│       ├── sqlite/connection.ts   # 连接/事务/迁移/PRAGMA/run-get-all
│       ├── repositories/          # 5 个按域仓库(image/prompt/tag/tagGroup/stats),SQL 只在这里
│       ├── fsPort.ts               # node fs 语义端口(全量实现,结构化类型满足各服务窄端口)
│       ├── imageFiles.ts          # 缩略图/存图/MD5
│       ├── tagCache.ts            # 全标签缓存
│       └── backup.ts              # zip/目录遍历/孤儿文件/备份进度推送
├── preload/
│   └── index.ts                   # 安全桥:bridge(key) 按契约桥接通道;日志便捷方法;推送事件订阅
├── renderer/                      # 渲染进程(视图层 = interfaces 的 UI 侧)
│   ├── app.ts                     # 组合根:构造全部 Manager;不含业务逻辑
│   ├── app.types.ts               # 12 个窄 port(ManagerDeps)+ IApp(App 类自身形状)
│   ├── constants.ts               # UI 常量(ElementId/ICONS/Events)
│   ├── features/                  # 垂直切片(新功能首选落点)
│   │   └── batch-toolbar/         # 批量工具栏(原 src/middle)
│   ├── managers/                  # 既有 UI 管理器(面板/详情/标签/回收站等 26 个)
│   ├── services/                  # 用例服务(TagService/ImageUploadService/DialogService/DataClearApi)
│   ├── renderer_utils/            # UI 技术件(VirtualScroller/ShortcutManager/SaveManager/…)
│   ├── components/                # 通用组件(ProgressDialog/EditableTagList)
│   └── configs/                   # LocalStorage 配置
├── shared/                        # 跨进程共享(零依赖,仅允许引用 ../types)
│   ├── ipc-contract.ts            # ★ IPC 契约:通道名 + 全通道签名 + 完备性断言
│   └── domain/                    # 领域类型:database-types.ts(Prompt/Image/行类型)、trashType.ts
├── lib/                           # 纯逻辑库(可独立测试/提取,零依赖)
│   ├── tag-groups/                # 标签领域规则(operations/validation/TopGroupManager)
│   └── batch-toolbar/             # 批量工具栏预设配置
├── types/
│   └── entities.ts                # 线格式视图(IPrompt/IImage),与 domain 领域模型结构兼容
└── utils/                         # 跨进程共享工具(纯函数为主:时间/id/FileUtils/EventBus/LRUCache…)
```

测试与配置:`tests/`(vitest,按域分组;用例服务单测在 `tests/main/application/`)、`e2e/`(Playwright + factories)、`scripts/check-constants.ts`(ID 命名检查)。

---

## 二、依赖方向(lint 强制,违反即报错)

```
renderer/preload ──✗──> main           (no-restricted-imports)
shared ──✗──> 其他任何层               (仅允许 ../types)
main/interfaces ──✗──> renderer        (no-restricted-imports)
```

| 规则 | 落点 |
|---|---|
| 渲染进程/preload 禁止引用主进程模块 | 共享类型放 `src/shared` 或 `src/types` |
| shared 层零依赖 | 不得引用其他任何层(白名单:`../types`) |
| 主进程 IPC 路由禁止引用渲染进程模块 | 路由不得 import renderer 下任何文件 |

隐含方向(靠 review 保证):`interfaces → infrastructure` 只允许经 `database.ts` facade 或显式基础设施模块;`domain`(`shared/`、`lib/`)不得反向 import 任何上层。

---

## 三、新增代码指南(按场景)

### 场景 1:新增一个 IPC 通道

按顺序动 3 处,缺一不可:

1. `src/shared/ipc-contract.ts`:`IPC` 加一行通道名 + `IpcApi` 加一条方法签名。**漏签名会在契约的编译期完备性断言(`AssertIpcComplete`)直接报错**;
2. `src/main/interfaces/ipc/<域>Router.ts`:`handleTyped("通道键", handler)`。入参/返回类型由契约推导,返回值与签名不符会报错——签名必须描述 main 真实返回;
3. `src/preload/index.ts`:对象里加一行 `通道键: bridge("通道键")`。`satisfies IElectronAPI` 会强制补齐。

渲染进程直接 `window.electronAPI.通道键(...)`,类型自动生效,**不要**手写 `ipcRenderer.invoke("字符串")` 或 `ipcMain.handle("字符串")`。

推送事件(主进程主动 send)走 `IPC_EVENTS` 常量,渲染侧用 `on/offXxx` 方法订阅。

新增通道时问自己:这是不是死通道?——main 注册了但 preload 没桥接、或渲染进程从不调用的通道不要加(重构时清理过 6 个这类遗留)。

### 场景 2:新增/修改实体字段

双端类型必须**同一次改动**里同步,这是历史上踩过最深坑的边界(isFavorite 一边 boolean 一边 number 共存了很久):

- 领域模型(主进程真实形状):`src/shared/domain/database-types.ts` 的 `Prompt/Image`;
- 线格式视图(渲染进程声明):`src/types/entities.ts` 的 `IPrompt/IImage`;
- 两者的结构必须保持兼容(main 类型可赋值给 entities 视图),契约与 `handleTyped` 会仲裁;
- 字段语义约定:`isFavorite` 是 boolean 不是 0/1;`thumbnailPath/width/height/deletedAt` 会是 `null`,视图类型必须认领;
- 命令入参(新增/更新的 payload)用 `Create*Params/Update*Params`(domain),与实体类型的差异是合理的,不要强行合并。

### 场景 3:新增/修改 UI 功能

- **全新功能** → `renderer/features/<功能名>/`,自带 index.ts 对外单向导出;切片之间不直接 import,跨切片通信走 eventBus 或显式窄接口;
- **修改现有面板/详情/标签/回收站** → 找到对应 Manager;Prompt 与 Image 的平行逻辑优先改 `PanelManagerBase`/共享组件,不要在两个子类里复制;
- **新 Manager 的依赖**:在 `app.types.ts` 声明窄 port(`interface XxxDeps { … }`),构造注入;**禁止 `app: any`**,也不得把整个 `IApp` 传给新代码——每个组件只声明它真正用到的成员;
- 需要触达其他 Manager 的能力时,优先在 eventBus 上发事件或给窄 port 加方法,不要伸手拿 `app.xxxManager` 内部状态。

### 场景 4:新增主进程业务逻辑

- **薄透传**(校验后直接调一次仓库)→ 直接写在对应域的路由里;
- **出现跨仓储编排、事务性用例** → 提成 `main/application/` 下的用例服务,路由保持薄。范本:`ImportFullBackupService.ts`(校验 → 备份当前数据 → 恢复 → 失败自动回滚),依赖全部构造注入(fs 聚合端口 + 数据库生命周期 + 进度回调),路由只负责文件对话框和错误翻译;
- **用例装配集中在 `application/index.ts`**(组合根):服务无状态、单例化;fs 一律用 `infrastructure/fsPort.ts` 的 `nodeFsPort`(全量语义端口,按结构化类型满足各服务窄端口),新增服务注册一行即可,不要在路由里手写 fs 适配器;
- **服务如何可单测**:像 ImportFullBackupService/ExportFullBackupService/ExportOrphanFilesService 一样把"文件系统/数据库/时间戳"设计成注入端口,测试用 fake 模拟失败场景(回滚、清理、进度序列)——这类路径 e2e 构造不出来;
- **SQL 只写进 `infrastructure/repositories/`**,并同步到 `database.ts` facade 的导出面;连接/事务/迁移问题改 `sqlite/connection.ts`,不要在仓库里直接碰 `db` 连接(需要预处理语句时用 `getDb()`);
- 文件系统操作(存图/缩略图/zip)→ `infrastructure/` 对应模块,路由不直接 `fs`;
- 主进程可变状态(mainWindow/dataDir/缓存)→ 加到 `runtime.ts` 或对应基础设施模块的访问器,**不要新建模块级可变全局**。

### 场景 5:新增纯逻辑 / 类型

- 标签等领域规则 → `lib/tag-groups/`(带独立单测);
- 其他可独立复用的纯函数库 → `lib/`;
- 跨进程共享的通用工具(时间/字符串/文件路径)→ `utils/`;
- 跨进程领域类型 → `shared/domain/`;仅渲染进程用的视图类型 → `types/entities.ts` 或就近;
- UI 常量(ElementId/图标/文案)→ `renderer/constants.ts`;跨进程领域常量(如 TrashType)→ `shared/domain/`。

---

## 四、编辑既有代码的注意事项

1. **不要用字符串直连 IPC**。契约是通道名单一事实源:改名改的是 `IPC` 常量,两端自动跟随;grep 不到 `"xxx-xxx"` 字面量才算干净(`shared/ipc-contract.ts` 除外)。
2. **不要放宽契约来迁就实现**。`handleTyped` 报"返回类型不符"时,先问 main 实际返回什么——契约描述线上真实行为;确实声明错了(如 void vs boolean)就修契约,而不是在 handler 里 `as any`。历史教训:18 个通道的 void 谎言就是这么攒出来的。
3. **`as unknown as` 是债务标记**。目前仅存 3 处命令方向适配(addPrompt/updatePrompt/updateImage),新代码不得新增;发现別处的全量断言优先考虑类型修正。
4. **Manager 依赖只增不改语义**。给某个 port 加成员是安全的;把成员从 port 移除前,先 grep 该 Manager 全部 `this.app.` 访问。
5. **renderer/constants.ts 与 shared 的边界**:ElementId、ICONS、Events 是 UI 概念,主进程不需要也不得接触;发现 main 需要某个常量时,把它提升到 `shared/domain/`(参考 TrashType),而不是让 main import renderer。
6. **eventBus 事件名**在 `renderer/constants.ts` 的 `Events` 中注册,不要散写字符串。
7. **提交流程**:`pnpm check`(constants 命名检查 + typecheck + oxfmt + lint)全绿后再提交;行为改动跑 `pnpm test` + 相关 `e2e` 文件。
8. **e2e 已知偶发用例**:`3-image-detail-database-fields`、`9-tag-manager`、`7-main-card-multi-select` 偶发超时失败——全量跑挂了先**单跑该文件**复核,单跑过即视为通过。
9. **构建入口**:electron-vite 三个 bundle(main/preload/renderer)按 entry 从各自目录解析;不要让 main 的代码被 renderer import(反之亦然),否则会破坏打包与安全模型。
10. **`lib/` 与 `utils/` 的区别**:`lib/` 是领域规则库(可独立测试、未来可提取为包);`utils/` 是跨进程工具箱。拿不准时:规则/业务语义 → lib,纯技术函数 → utils。
