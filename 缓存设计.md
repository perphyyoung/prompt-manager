# 缓存设计

> 本文描述渲染进程缓存体系的当前设计与演进思路。
> 数据契约前提：**面板权威数据源是 `filteredImages` / `filteredPrompts` 内存数组**
> （随分页加载增长），CacheManager 中的 LRU 一律是纯加速层，
> 所有读取必须有 `|| 兜底`，不允许作为唯一数据源依赖。

## 一、缓存清单

| 名称 | 容量 | 内容 | 主要写入点 | 主要读取点 | 淘汰后果 |
|---|---|---|---|---|---|
| `images` | 500 条 | 完整 IImage | loadData/loadMore 每页 `cacheImage`；refreshIncremental 重写；详情保存 `updateCachedItem(InPlace)` | 详情页取"最新对象"、SaveStrategy 合并、hover 兜底 | 无功能影响（读取处均有兜底），仅损失加速 |
| `prompts` | 500 条 | 完整 IPrompt | 同上（prompt 版） | 同上（prompt 版） | 同上 |
| `thumbnailPaths` | 5000 键 | id → 缩略图绝对路径 | 分页后 `prefetchImagePaths`（主入口）；卡片背景渲染未命中时批量兜底写入 | 卡片背景渲染（每张卡片）、回收站缩略图 | 该卡片背景走一次 IPC 批量兜底查询 |
| `originalPaths` | 1000 键 | id → 原图绝对路径 | hover 预览未命中时写入 | **仅 HoverTooltipManager**（hover 原图预览） | 下次 hover 重查一次 IPC（有 500ms debounce，无感） |
| `promptRefImagesCache` | 100 条 | 提示词详情关联图编辑会话状态 | 详情管理器多处 set/clear | 详情关联图列表渲染与校验 | 仅影响当前编辑会话 |

键约定：路径类缓存的键为**纯 imageId**（原图/缩略图由所属缓存区分）；
读取统一用 `peek()`（不扰动 LRU 淘汰顺序）；写入用 `set()`。

## 二、关键设计决定

### 1. 路径缓存按访问频率拆分为两个独立 LRU
缩略图路径是每张卡片的必读项（高频），原图路径只有 hover 预览使用（低频）。
合并放在一个 LRU 里会互相挤占——旧实现 500 键混装实际只能覆盖约 250 张图。
拆分后缩略图可覆盖万级库。

### 2. 原图路径缓存只覆盖 hover，不覆盖详情界面（有意为之）
图像详情大图 / 全屏查看 / 提示词关联图预览（ImageDetailManager、
ImageFullscreenManager、PromptDetailManager）为**低频单次查询**，
直接经 `electronAPI.getImagePath(relativePath)` 获取，不经过缓存。
理由：relativePath → fullPath 在主进程开销极小，单次查询无感；
接入缓存反而引入"数据目录迁移/重命名后旧路径失效"的管理负担。
若未来出现详情内频繁切换图片且路径 IPC 可感知的场景，再接入并上调容量。

### 3. 面板数据源与缓存解耦（重要）
拖标签、全选、批量收藏等一切按 id 查找，一律查 `filteredImages/filteredPrompts`，
**禁止**回退到元数据 LRU（历史上曾因此出现"滚动淘汰后拖标签报项目不存在"）。
LRU 中软删除等残留条目无读取路径，由 LRU 自然淘汰。

### 4. 同步链路
```
字段保存 / 批量操作
→ emit PROMPTS_CHANGED / IMAGES_CHANGED
→ refreshAfterUpdate → refreshIncremental
   ├─ 重写 filtered 数组 + 重写对应 LRU（cacheImage/cachePrompt）
   ├─ 指纹 diff：仅重建变化的可见卡片
   └─ ensureWindowData：虚拟窗口数据补齐
```

## 三、已知权衡与限制

1. **元数据 LRU 仍是"半活跃"状态**：它已是纯加速层，但约 20 个读写点
   （getCachedImage/getCachedPrompt/updateCachedItem 等）仍散布在
   PromptDetailManager / ImageDetailManager / SaveStrategy / app.ts 中。
   新增代码时应默认**不新增**对它的依赖。
2. **stale read 放大器**：LRU 会残留旧版本对象。任何新的写入路径若忘记发事件
   或漏写缓存，旧值会存活到自然淘汰为止（万级库高频浏览下窗口很短）。
3. **双实例编辑竞态**：同一提示词的两个详情先后保存为后写覆盖——这是字段级
   自动保存协议的固有问题，与缓存无关，本方案的改进也不覆盖（需乐观锁）。
4. 旧 `imagePaths` 缓存实例在改版后的首个会话内以死数据形式存在，重启即消失。

## 四、已实施的优化记录

- 路径缓存拆分（thumbnailPaths 5000 / originalPaths 1000）、键简化、peek 化
- 移除失败路径全量 clear（loadData catch ×2、app.loadPrompts catch、
  两面板 delete 配置 clearCache、DetailViewManager 删除后 clear）
- 新增 `cacheImagesAppend` 追加式写入；提示词详情选择图像不再 clear+fill 全局缓存
- 批量加标签/批量收藏下沉为主进程集合 SQL（配合虚拟滚动的万级全选场景）

## 五、改进思路（backlog）：结构性收敛

**定位：默认不排期。** 触发条件满足前不要专项实施：
- 触发条件 a：详情链路再出现实际的数据一致性 bug（跨入口编辑互相覆盖等）
- 触发条件 b：需要做内存画像优化
- 日常维护策略：触碰详情链路相关代码时，顺手把对应 LRU 读写改为查 filtered 权威

**内容**：退役 images/prompts 元数据 LRU 的读写点。
详情/保存链路需要的"最新对象"改由事件携带或从 filtered 数组查找；
CacheManager 只保留两个路径缓存，promptRefImagesCache 保留为详情局部状态。

**收益**：单一数据源，消除双写不一致的根本混乱与心智负担。

**代价评估**：触碰约 20 个读写点（getCachedImage/getCachedPrompt/
updateCachedItem/cachedImages 等，集中于 PromptDetailManager ≈1400 行、
ImageDetailManager、SaveStrategy、app.ts），需回归详情页全部交互
（图像选择器、关联图编辑、字段自动保存、hover 预览）。

**不实施的后果评估（当前结论）**：日常使用不可感知——
性能无开销（O(1)）、内存上限几 MB、正确性靠事件闭环与全量兜底维持。
真正的风险是维护性的：未来改动引入不同步时，症状为偶发的界面显示旧数据，
难复现难排查。
