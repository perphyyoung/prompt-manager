# 通用语言（Ubiquitous Language）

本文档定义了领域内的通用语言，确保团队成员使用一致的术语进行交流。

## 限界上下文

### 1. 内容管理（Content Management）

**上下文描述**：用户创建、编辑、组织和查找提示词与图像的核心领域。

| 中文术语 | 英文术语 | 定义 | 领域事件 | 避免使用的别名 |
|----------|----------|------|---------|---------------|
| **提示词** | Prompt | 用户保存的 AI 提示文本及其关联元数据 | 提示词已创建、提示词已更新、提示词已删除、提示词已复制 | 咒语、指令 |
| **图像** | Image | 与提示词关联的图片文件 | 图像已上传、图像已关联、图像已取消关联 | 图片、照片 |
| **标签** | Tag | 用于分类提示词和图像的关键词 | 标签已应用、标签已移除 | 标记、分类 |
| **特殊标签** | Special Tag | 系统预设的具有特殊功能的标签，见下表 | — | 系统标签、预设标签 |

**特殊标签清单**：

| 中文术语 | 英文术语 | 适用对象 | 定义 | 触发条件 |
|----------|----------|----------|------|----------|
| **收藏** | Favorite | 提示词、图像 | 用户标记为重要的内容 | 用户手动标记 |
| **未引** | Unreferenced | 图像 | 未被任何提示词引用的图像 | 系统自动计算 |
| **多引** | Multi-Referenced | 图像 | 被多个提示词引用的图像 | 系统自动计算 |
| **无图** | No Image | 提示词 | 未关联任何图像的提示词 | 系统自动计算 |
| **多图** | Multi-Image | 提示词 | 关联多张图像的提示词 | 系统自动计算 |
| **安全** | Safe | 提示词、图像 | 内容适合所有场合 | 用户手动标记 |
| **敏感** | Sensitive | 提示词、图像 | 内容可能不适合工作场合 | 用户手动标记 |
| **无标** | No Tag | 提示词、图像 | 未添加任何标签的内容 | 系统自动计算 |
| **单语** | Single Language | 提示词 | 无有效翻译内容的提示词 | 系统自动计算 |

**领域关系**：

- 一个**提示词**可以关联多张**图像**（多对多）
- 一个**图像**可以关联多个**提示词**（多对多）
- 一个**提示词**或**图像**可以有多个**标签**

### 2. 视图展示（View Presentation）

**上下文描述**：用户浏览和查看内容的界面呈现方式。

| 中文术语 | 英文术语 | 定义 | 避免使用的别名 |
|----------|----------|------|---------------|
| **网格视图** | Grid View | 以卡片网格形式展示项目的视图模式 | 卡片视图、缩略图 |
| **详情界面** | Detail View | 显示单个项目详细信息的界面 | 详情页、弹窗、详情管理器 |
| **全屏界面** | Fullscreen View | 以全屏模式查看图像的功能 | 全屏 |

### 3. 标签筛选（Tag Filtering）

**上下文描述**：用户通过标签过滤和查找内容的交互区域。

| 中文术语 | 英文术语 | 定义 | 领域事件 | 避免使用的别名 |
|----------|----------|------|---------|---------------|
| **标签筛选区** | Tag Filter Area | 展示所有可用标签供用户选择的界面区域 | 标签筛选区已展开、标签筛选区已收起 | 标签栏、标签面板 |
| **收起状态** | Collapsed State | 标签筛选区收起时的紧凑显示模式 | — | 收起、折叠状态 |
| **展开状态** | Expanded State | 标签筛选区展开时的完整显示模式 | — | 展开模式 |
| **标签项** | Tag Item | 单个可点击的标签选项 | 标签项已选中、标签项已取消选中 | 标签按钮、标签选项 |
| **首位组** | Top Group | 排序第一位的标签组，其标签在收起时全部显示 | — | 主组、第一个组 |
| **标签组** | Tag Group | 用于分类标签的分组容器 | 标签组已创建、标签组已更新、标签组已删除 | 标签分类、标签集合 |
| **标签组卡片** | Tag Group Card | 标签管理界面中显示单个标签组的卡片组件 | — | 标签卡片、组卡片 |
| **未分组** | Ungrouped | 不属于任何标签组的标签 | — | 无分组、无分类 |
| **选中标签** | Selected Tags | 用户当前选中的用于筛选的标签集合 | 选中标签已变化 | 激活标签、筛选标签 |
| **标签计数** | Tag Count | 显示每个标签关联的项目数量 | — | 标签数量 |
| **清空筛选** | Clear Filter | 取消所有已选标签的操作 | 筛选已清空 | 重置筛选 |

**领域规则**：

- **标签筛选区**可以展开或收起
- 选中**标签项**后，内容区域只显示包含该标签的项目
- 多个**选中标签**之间为"与"关系（交集）
- **标签计数**实时反映当前筛选条件下的项目数量

### 4. 批量操作（Batch Operations）

**上下文描述**：用户同时处理多个项目的场景。

| 中文术语 | 英文术语 | 定义 | 领域事件 | 避免使用的别名 |
|----------|----------|------|---------|---------------|
| **批量模式** | Batch Mode | 允许同时选择多个项目进行操作的模式 | 已进入批量模式、已退出批量模式 | 多选模式 |
| **批量工具栏** | Batch Toolbar | 提供批量操作功能的界面区域 | — | 批量操作栏 |
| **添加标签** | Add Tags | 为多个项目同时添加标签的操作 | 批量标签已添加 | 批量打标签 |
| **删除** | Delete | 同时删除多个项目的操作 | 批量删除已执行 | 批量移除 |

**领域规则**：

- **批量工具栏**仅在**批量模式**下可见
- **添加标签**和**删除**是**批量工具栏**提供的功能

### 5. 系统配置（System Configuration）

**上下文描述**：用户个性化应用外观和行为的设置。

| 中文术语 | 英文术语 | 定义 | 领域事件 | 避免使用的别名 |
|----------|----------|------|---------|---------------|
| **设置界面** | Settings | 显示应用设置选项的界面 | 设置界面已打开、设置界面已关闭 | 设置弹窗、设置窗口、设置管理器 |
| **应用版本** | App Version | 应用的当前版本号，通过 Electron 的 app.getVersion() API 获取并显示在设置界面标题旁 | — | Version |
| **本地存储** | Local Storage | 用户设置的持久化数据（主题、字体等） | 本地存储已保存、本地存储已加载 | 本地缓存 |
| **明暗主题** | Theme | 应用的明暗色彩方案 | 主题已切换 | 皮肤、外观 |
| **字体** | Font | 应用界面使用的字体家族 | 字体已更改 | 字形、字型 |
| **字体大小** | Font Size | 应用界面的文字缩放比例 | 字体大小已调整 | 字号 |
| **文字样式** | Text Style | 卡片上文字的颜色设置 | 文字样式已更改 | 卡片文字颜色 |
| **数据目录** | Data Directory | 应用数据存储的文件夹位置 | 数据目录已更改 | 存储路径 |
| **安全模式** | Safe Mode | 过滤敏感内容的视图模式 | 安全模式已切换 | SFW模式 |
| **自定义字体** | Custom Font | 用户上传的字体文件 | 自定义字体已添加 | 私有字体 |

### 6. 数据管理（Data Management）

**上下文描述**：数据备份、恢复和维护的领域。

| 中文术语 | 英文术语 | 定义 | 领域事件 | 避免使用的别名 |
|----------|----------|------|---------|---------------|
| **孤儿文件** | Orphan Files | 数据库中无记录但实际存在的文件 | 孤儿文件已发现 | 孤立文件 |
| **完整备份** | Full Backup | 包含所有数据和配置的导出文件 | 完整备份已导出、完整备份已导入 | 全量备份 |
| **清空数据** | Clear Data | 删除所有应用数据和设置的操作 | 数据已清空 | 重置数据 |

## 示例对话

### 批量操作

> **开发**："图像网格视图, 当用户切换到**批量模式**时，**批量工具栏**应该显示哪些操作？"
> **产品经理**："**批量工具栏**应该显示**添加标签**和**删除**等按钮。用户可以选择多个**图像**，然后统一操作。"

## 术语对照表

| 代码中的命名 | 领域术语 | 说明 |
|-------------|---------|------|
| PromptManager | 内容管理 | 代码实现使用 Manager 后缀，领域语言使用业务术语 |
| SettingsManager | 设置界面 | 同上 |
| Modal | 界面 | 技术实现术语，领域语言使用"界面" |
| BatchManager | 批量操作 | 同上 |
| TagFilter | 标签筛选区 | 同上 |

## 标记的歧义

- **"管理器"** 在代码中广泛使用（PromptManager, ImageManager等），但在领域语言中应使用业务术语如**内容管理**、**设置界面**
